import 'package:flutter/material.dart';

class WasteScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Waste Management'),
      ),
      body: Center(
        child: Text('Waste Screen'),
      ),
    );
  }
}